import Link from 'next/link'
import { Calculator, Clock3, FileQuestion } from 'lucide-react'
import { MarketplaceListingActions } from '@/components/marketplace/marketplace-listing-actions'
import { getMarketplaceListing } from '@/lib/marketplace'

export const dynamic = 'force-dynamic'

export default async function MarketplaceListingPage({ params }: { params: Promise<{ slug: string }> }) {
  const listing = await getMarketplaceListing((await params).slug)
  const price = listing.pricing_type === 'free' ? 'Free' : new Intl.NumberFormat('en-NG', { style: 'currency', currency: 'NGN', maximumFractionDigits: 0 }).format(listing.price_kobo / 100)
  return <main className="mx-auto max-w-5xl px-4 py-10 pb-24 sm:px-6 lg:py-14"><Link href="/mocks" className="text-sm font-medium text-[#2e2877]">← Explore mocks</Link><section className="mt-5 overflow-hidden rounded-3xl border border-[#e3ded9] bg-white"><header className="bg-[#2e2877] px-6 py-8 text-white sm:px-10 sm:py-11"><p className="text-sm text-white/70">{listing.examination || listing.subjects.join(' · ') || 'Practice mock'}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">{listing.title}</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-white/75">{listing.short_description}</p></header><div className="grid gap-8 p-6 sm:p-10 lg:grid-cols-[1fr_300px]"><div><h2 className="text-xl font-semibold">What to expect</h2>{listing.instructions && <p className="mt-4 whitespace-pre-wrap text-sm leading-7 text-[#5f5964]">{listing.instructions}</p>}<div className="mt-6 flex flex-wrap gap-2">{listing.subjects.map(subject => <span key={subject} className="rounded-full bg-[#f0edff] px-3 py-1 text-xs font-medium text-[#2e2877]">{subject}</span>)}</div><p className="mt-7 text-sm text-[#716c76]">Created by {listing.creator?.first_name ? `${listing.creator.first_name} ${listing.creator.last_name}` : listing.creator_school?.name || 'a Kanvise tutor'}{listing.creator_school?.name ? ` · ${listing.creator_school.name}` : ''}</p></div><aside className="rounded-2xl bg-[#f7f4f1] p-5"><p className={`text-2xl font-semibold ${listing.pricing_type === 'free' ? 'text-[#29724b]' : 'text-[#29262f]'}`}>{price}</p>{listing.checkout_summary && <div className="mt-3 border-t border-[#ded8d2] pt-3 text-xs text-[#716c76]"><div className="flex justify-between"><span>Mock price</span><span>{new Intl.NumberFormat('en-NG', { style: 'currency', currency: 'NGN', maximumFractionDigits: 0 }).format(listing.checkout_summary.mock_price_kobo / 100)}</span></div><div className="mt-1 flex justify-between"><span>Paystack processing charge</span><span>{new Intl.NumberFormat('en-NG', { style: 'currency', currency: 'NGN', maximumFractionDigits: 0 }).format(listing.checkout_summary.processing_fee_kobo / 100)}</span></div><div className="mt-2 flex justify-between border-t border-[#ded8d2] pt-2 font-semibold text-[#29262f]"><span>Total</span><span>{new Intl.NumberFormat('en-NG', { style: 'currency', currency: 'NGN', maximumFractionDigits: 0 }).format(listing.checkout_summary.total_kobo / 100)}</span></div></div>}<dl className="mt-5 space-y-4 text-sm"><div className="flex justify-between gap-3"><dt className="flex items-center gap-2 text-[#716c76]"><FileQuestion size={16}/>Questions</dt><dd className="font-semibold">{listing.question_count}</dd></div><div className="flex justify-between gap-3"><dt className="flex items-center gap-2 text-[#716c76]"><Clock3 size={16}/>Time</dt><dd className="font-semibold">{listing.duration_minutes ? `${listing.duration_minutes} mins` : 'Untimed'}</dd></div><div className="flex justify-between gap-3"><dt className="flex items-center gap-2 text-[#716c76]"><Calculator size={16}/>Calculator</dt><dd className="font-semibold capitalize">{listing.calculator_mode}</dd></div><div className="flex justify-between gap-3"><dt className="text-[#716c76]">Attempts</dt><dd className="font-semibold">{listing.attempts_included}</dd></div></dl><div className="mt-6"><MarketplaceListingActions listingId={listing.id} free={listing.pricing_type === 'free'} /></div></aside></div></section></main>
}
